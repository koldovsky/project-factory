// Fixture: product code EXISTS but there is no trace/ledger.jsonl.
// Post-phase emptiness => the digest must render NOT-EARNED (exit 1),
// never PASS (prime directive: absence of evidence is not success).
console.log("real product code");
