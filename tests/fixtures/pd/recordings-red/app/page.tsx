// Synthetic product code — its mere presence puts the tree past Phase 6, so an
// empty recordings base must render NOT-EARNED (PD-4), never a bare PASS.
export default function Page() {
  return <main>Synthetic home page (PD-4 red fixture).</main>;
}
